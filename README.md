# movies-system

Install packages
```
npm i 
```

Run project
```
npm start
```

Create .env file on root directory
```
DB=movies_system
DBUSER=youruser
PASSWORD=yourpassword
HOST=localhost
```
PSD: Create a db with .sql file in the project.
